# 模塊層・結構層・練習層規範 v0.2

狀態:草案,R6–R8 裁決後凍結
取代:《結構層與練習層 v0.2 變更清單》
證據基礎:SRC-001(ML 基礎教學文)人工垂直切片實測
適用範圍:資訊分流管線中「模塊化 → 編碼 → 結構化 → 練習生成」四段;不涵蓋文件正規化、技能 Registry、熟練度模型

---

## 第一章 設計原則

**P1 模塊是索引面,不是閱讀面。**
模塊層是原文的有損索引。任何下游需要原味內容(尤其練習素材)必須經 `char_span` 回原文取材,禁止只消費模塊摘要。

**P2 耗損是功能,但必須分層量測。**
實測基線(SRC-001,~65 條原子命題):概念 ~95%、機制鏈 ~90%、量化細節 ~50%、類比/記憶鉤 ~15%、敘事順序 ~30%。籠統的總保留率無意義;驗收一律分層報告。

**P3 缺的資訊用邊補,不用更細的節點補。**
敘事與關係層的耗損(30%)由有類型的邊挽回(→80%+),增量成本約為模塊化的 15%。禁止以縮小節點粒度(心智圖式短標籤)換取結構——那會摧毀機制鏈的 90% 保留率。

**P4 空欄位必須有語義。**
一個欄位為空,只允許表達「作者未提供」;「此型別不適用」必須由 schema 分型消除,不得由空值承擔。(本條是 §2 分層 schema 的直接動機。)

**P5 匹配引擎只讀核心投影。**
技能對齊永遠面對同一種語言(ModuleCore);型別分化不得污染對齊層。

**P6 證據先於規格。**
任何新型別本體、新邊類型、新 motif,須以基準標註數據為前提。無數據不開規格。

---

## 第二章 模塊層

### 2.1 分層 schema(R9)

```text
ModuleCore(凍結;改動 = breaking change)
├─ module_id / source_id / char_span
├─ article_type / semantic_roles
├─ domain / operations / problem_type
├─ input_type / output_type
├─ cognitive_level / is_skill_signal
└─ confidence / schema_version / extractor

型別本體(隨型別入凍結集時一次性定義)
├─ TeachingModule : concept_core, examples, hooks, limitation
├─ MethodModule   : procedure, constraints(必填,允許值「作者未給」),
│                   preconditions, failure_modes
├─ CaseModule     : context, actions(含事件序), outcome, transferability
└─ AnalysisModule : claim, evidence, assumptions, counterarguments
```

- 實作:base class + 子類(Pydantic),單日工作量。
- 匹配引擎僅准讀 ModuleCore(P5);資產提升與練習生成方可讀型別本體。
- `hooks`(類比、口訣、關鍵數值範例)為教學型本體欄位:對匹配零價值、對練習高價值,可將練習層耗損自 ~50% 壓至 ~25%。

### 2.2 split 規則(R2)

一個模塊可有多個 semantic roles,但不可有多條互不相關的對外 depends_on。**異源依賴 >1 → 強制 split。**
(實證:M13 三條建議各依賴 M11、M05、無;合寫時邊層無法成立。)

### 2.3 弱策略點規則(R4,依 2.1 修訂)

MethodModule 之 `constraints` 值為「作者未給」→ 弱策略點:
1. 禁止生成 Apply 型練習(無邊界條件的規則練習訓練過度泛化)。
2. 替代路徑:(a) 降轉 Unknown 進 Problem Model,待第二來源補條件後升級;(b) 生成「邊界獵取」型 Evaluate 題(此規則何時失效)。

### 2.4 非技能訊號

純背景、鋪陳、行銷內容標記 `is_skill_signal = false`,保留於模塊層(維持 char_span 連續性)但不進入對齊與資產流程。

---

## 第三章 結構層(邊)

### 3.1 邊詞彙(核心 + 型別擴充)

```text
核心(全型別):depends_on / exemplifies / elaborates /
              equivalent_to / contrasts / motivates / solves
型別擴充:
  教學・敘事型:foreshadows
  方法型(預留):condition(自 RST 引入,待基準數據)
  案例型(預留):事件因果邊(待 30 篇中案例型實測後裁決)
```

- equivalent_to 無方向;其餘依 3.2。
- foreshadows 為唯一無標準標註傳統者(源自敘事學),LLM 抽取穩定性須單獨驗證。
- 擴充詞彙前先查 RST 關係清單(~25 種)及其標註一致性數據;人類標不穩定的關係,LLM 產出即雜訊,不得引入。

### 3.2 方向規約(R1)

箭頭指向被支撐者(子→父、後→前)。例:M09 —solves→ M08、M03 —exemplifies→ M02。

### 3.3 維度覆蓋(邏輯・因果・時序)

| 維度 | 載體 | 說明 |
|---|---|---|
| 功能因果 | 邊:solves / motivates / depends_on | 模塊間粗粒度因果 |
| 細粒度推理鏈 | 模塊內 mechanism 角色 | 不上邊層,防圖爆炸 |
| 邏輯等價/實例/對立 | 邊:equivalent_to / exemplifies / contrasts | — |
| 蘊涵・條件 | 暫缺;方法型預期需要 | 屆時引入 RST 之 CONDITION |
| 文本順序 | char_span 線性層 | **禁止設 then/next 邊**(資訊重複且誘發全連接濫標) |
| 敘事時間 | 邊:foreshadows | 線性順序推不出的鋪陳—回收 |
| 事件時序(案例) | 暫由 case 角色集(context/action/outcome)隱含 | 升格與否待實測 |

### 3.4 抽取程序:兩遍式

1. 第一遍:相鄰與近距模塊逐對局部判斷。全局結構(脊柱、樞紐)為湧現結果,不做全局設計。
2. 第二遍:全模塊摘要一次全局掃描,專抓長程邊(伏筆、遠距等價)。實測佔比 1/12,成本可控。

### 3.5 無章節降級鏈(R3)

`章節樹 →(缺席)段落序列 →(保底)char_span 線性順序`。
章節帶是排版鷹架,非承重結構;任何輸入下「模塊可回原文」驗收恆成立。結構越差的來源,邊層挽回率越高。

### 3.6 免費副產品(准用,不另建機制)

- 度數 = 重要性(入邊數即樞紐訊號,供資產優先序)。
- 邊層反向偵測 split(2.2)。
- 圖結構參數(鏈長、星入邊數、拔點深度)= 練習難度刻度。

### 3.7 心智圖定位

本結構為概念圖(節點 + 有標籤有向邊)。心智圖是其有損投影(收起語義邊、只留章節樹、Markmap 渲染),僅作 Dynamic View;反向重建不成立,故儲存格式必須是圖。

---

## 第四章 練習層

### 4.1 單模塊題:role → 題型對照

| semantic role / 本體欄位 | 題型 |
|---|---|
| concept_core | Recall / Explain |
| mechanism | Explain / Diagnose |
| failure_mode | Diagnose |
| decision_rule / heuristic(有 constraints) | Apply |
| decision_rule(constraints=作者未給) | Evaluate(邊界獵取),依 2.3 |
| case(context/action/outcome) | Transfer |
| claim + evidence | Evaluate |

任何完成拆解的文章,練習產能不為零(最差保有 concept 與邊)。策略點自「練習系統入口」降級為本表一列。

### 4.2 跨模塊題:motif 文法

隨機性僅用於抽 motif 與抽子圖實例,**禁止隨機走圖**。

| Motif | 圖形 | 題型 | 組合約束 |
|---|---|---|---|
| 鏈 | A→B→C 因果邊串接 | 因果推理(補全/重排/必經步驟) | 僅 depends_on / motivates / solves 可為鏈段;exemplifies 不得居中 |
| 星 | 樞紐 + ≥3 入邊 | 抽象化(多例歸一) | 入邊須異源(不同 domain 或 role) |
| 反事實 | 刪一節點 | 結構理解(下游塌什麼) | 被刪節點須有 ≥1 出邊 |
| 斷邊 | 跨來源同技能模塊對 | 遷移(同構?矛盾?) | 兩來源須對齊同一技能節點 |

### 4.3 回填原則

斷邊題作答產出新邊(equivalent_to / contrasts):練習回填圖,不只消耗圖。進階形式:以使用者自有專案為第二來源(調度型、無標準答案)。

### 4.4 承重聲明

邊層為練習層承重結構。邊的錯誤下游放大(一條錯的 depends_on 生成一整族不成立的鏈題),故邊之驗收嚴於模塊:**寧缺勿錯**。

---

## 第五章 驗收與度量

**5.1 基準標註範圍(R8)**:30 篇基準每篇標註——文章類型、模塊邊界、semantic roles、型別本體必填欄位、**邊(ground truth)**、應產資產、應對齊技能。

**5.2 耗損指標**:每篇列原子命題清單,模塊化後檢查「命題可自模塊層恢復」比例,分五層(概念/機制/量化/類比/敘事)報告。作為不同 extractor(mock、LLM、prompt 版本)的比較基準。

**5.3 邊驗收**:除比例外另查——方向錯誤率、類型混淆矩陣(重點:elaborates vs exemplifies、depends_on vs motivates)、長程邊召回、foreshadows 穩定性。

**5.4 對齊驗收(沿用既有)**:錯誤自動對齊率、Top-5 召回、父子誤判率、同義誤建率;寧進人工覆核,不污染技能樹。

---

## 第六章 待裁決與凍結條款

**待 AOI 裁決(3 件)**
- R6:教學型入凍結集與否。入,則連同 TeachingModule 本體一併凍結(依 2.1)。
- R7:hooks 欄位——已依 2.1 落位為教學型本體欄位,裁決僅餘「採納與否」。
- R8:基準標註含邊層(工作量增幅估 15–20%)。

**凍結條款**
1. R6–R8 裁決後,本規範即為 v0.2 並凍結。
2. ModuleCore 改動一律 breaking change,須升 schema_version 並評估匹配引擎影響。
3. 型別本體僅於該型別入凍結集時一次性定義;後續修改依 P6。
4. v0.3 開啟條件:30 篇基準之實際標註數據。無數據不開新規格。
5. 未結案(高於本規範層級):本系統與「除 D1 外全部凍結」決定之優先序衝突,啟動實作前須裁決。

---

## 附錄A 概念族譜

RST(Mann & Thompson, 1988)關係層 × Novak 概念圖表示法 × Zettelkasten 模塊哲學,服務於 CASE 式技能樹。分析型角色集源自 Toulmin 論證模型;局部判斷—全局湧現為語篇剖析標準工法;長程邊獨立處理類比指代消解;度數中心性屬網路科學基礎。

## 附錄B SRC-001 實測摘要

14 模塊(1 非技能訊號)、12 邊(8 類型全數容納、1 長程)、樞紐 M05(入邊 ×4)、1 起 split 觸發(M13)、1 起弱策略點(M13-③)、對既有 5 節點種子技能樹全數 REJECT(正確行為)→ 產出 ~10 個 NEW_SKILL_PENDING,揭示「PENDING 累積多少同向訊號才開新分支」為技能樹層待補規則。
