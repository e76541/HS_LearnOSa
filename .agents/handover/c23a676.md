# 交接：AI-native 內聯互動知識圖

- 內容 commit：`c23a676`
- 分支：`cursor/inline-diagram-viz-plan-2c21`
- 日期：2026-07-14

## 完成內容

- 新增《How to Make a Company AI-Native》自包含 HTML fragment，作為只讀 Dynamic View。
- 提供三個共享 `selectedModuleId` 的分頁：成熟度旅程、完整關係網、旅程＋關係聚光。
- 成熟度旅程明示 M02 → M03 → M04 → M06 → M07 是閱讀順序，不冒充正式邊；另呈現 M00、M01、M05、M08 四個輔助模塊。
- 完整關係網呈現 M00–M08 與 E00–E18，保留 `source → target` 正式方向，並提供五種邊型別篩選與原生按鈕鍵盤替代介面。
- 詳情區依 Analysis、Teaching、Method、Case 本體顯示完整欄位，並列出模塊與邊的全部溯源資訊。
- 介面狀態只存在 fragment 執行期，不回寫來源、模塊、邊、索引或技能登記簿。

## 關鍵檔案

- `DOC/Review/how-to-make-company-ai-native/views/ai-native-knowledge-map.html`
- 只讀資料：同文檔目錄下 `source.md`、`modules.md`、`edges.md`、`views/_data.json`
- 規範入口：`Library/CURRENT.md`

## 驗證

- 前置資料檢查通過：9 個唯一模塊完整覆蓋 M00–M08；19 條唯一邊完整覆蓋 E00–E18；全部端點存在；每個模塊與邊均有證據區間。
- 瀏覽器實測三個分頁均可切換，M04 選擇跨分頁保留。
- 完整關係網預設顯示 9 個模塊與 19 條正式邊。
- 鍵盤 Enter 可切換分頁與選擇模塊；Space 可切換邊型別。關閉 `depends_on` 後正式邊由 19 條降為 11 條，重開後恢復 19 條。
- 選取 E06 後，主圖高亮、`M05 → M04`、`exemplifies` 與 `[7991,8360)` 證據同步。
- 736px 與 320px 實測無水平溢出、元素越界或控制項重疊。
- 控制台無 error 或 warning；fragment 32,612 bytes，未使用 fetch、XHR、WebSocket、固定定位、內部捲動區或 `broader_than`。
- `source.md`、`modules.md`、`edges.md`、`views/_data.json` 未修改。

## 接手入口

1. 讀 `Library/CURRENT.md`，再讀 canonical 模塊層、結構層與術語規範。
2. 讀 `.agents/skills/render-knowledge-views/SKILL.md`。
3. 直接開啟 `DOC/Review/how-to-make-company-ai-native/views/ai-native-knowledge-map.html`；它是 fragment，不是完整 HTML 文件。

## 工作樹注意事項

- 本次內容與交接提交只含知識圖 fragment 及本交接版本。
- 工作樹另有既存的 registry、canonical、計畫、`DOC/INDEX.md` 與 `DOC/Review/loop-and-harness-engineering/` 未提交變更；本次未修改、未暫存、未提交。

