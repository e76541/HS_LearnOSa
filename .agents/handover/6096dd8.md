# 交接：文檔路徑遷移與模塊篩選設計

- 內容 commit：`6096dd8`
- 分支：`cursor/inline-diagram-viz-plan-2c21`
- 日期：2026-07-14

## 完成內容

- 將 `docs/superpowers/specs/` 與 `docs/superpowers/plans/` 下七份既有文檔遷移至 `docs/specs/`、`docs/plans/`。
- 移除兩份計畫對已不存在 `superpowers:*` Skill 的強制依賴，保留逐項執行與核取方塊追蹤說明。
- 更新 `tools/viz/README.md`、`render-knowledge-views` Skill 與常駐篩選參考的現行路徑。
- 歷史交接不追改；`.agents/handover.md` 已明定舊版路徑按該版推送 ID 所在 commit 解析。
- 新增模塊篩選設計草案，規劃使用者硬性排除、系統軟性降權與動態忽略三層機制；第一版候選只處理已收錄來源的完整模塊。

## 篩選設計邊界

- 本次只有設計，未實作 `selection.md`、schema、Skill 或下游讀取器，也未新增 canonical 規範。
- 整篇來源不要仍走既有來源級拒絕，不進正式 DOC。
- 模塊級選擇只限制指定下游的消費資格；不修改 `source.md`、`modules.md`、`edges.md`，也不把偏好改標為 `pure_fill` 或 `background`。
- 候選 scope 為 `alignment`、`views`、`practice`、`speaking`、`all_downstream`；正式圖證據不因篩選而刪除。
- 每筆裁決只允許單一 scope；`exclude` 與 `defer` 第一版都由人工撤銷，`defer` 暫無自動期限。

## 關鍵檔案

- 設計草案：`docs/specs/2026-07-14-module-selection-design.md`
- 設計目錄：`docs/specs/`
- 實作計畫目錄：`docs/plans/`
- 常駐篩選參考：`.agents/handover/reference/filtering-and-module-size.md`
- 知識視圖 Skill：`.agents/skills/render-knowledge-views/SKILL.md`
- 交接規則與索引：`.agents/handover.md`

## 驗證

- `node --test tools/registry/test/*.test.mjs`：48/48 通過。
- `node --check` 驗證三個 `tools/viz` 入口腳本：通過。
- `git diff --cached --check`：通過。
- 現行檔案（排除版本化歷史交接）無 `docs/superpowers`、`superpowers:` 或 `REQUIRED SUB-SKILL` 殘留。
- 現行 `.agents` 與 `tools` 內引用的 `docs/specs/*.md`、`docs/plans/*.md` 路徑均存在。
- Git 將七份舊文檔辨識為 rename，未遺失正文。

## 接手與未決事項

1. 先讀 `Library/CURRENT.md`；篩選設計尚未 canonical，不得當成已啟用規則。
2. 若要進入實作，先建立 `docs/plans/2026-07-14-module-selection-implementation.md`，不要直接建立正式 `selection.md`。
3. 至少選一篇實際 DOC 來源驗證模塊排除、撤銷、斷鏈揭露與 bucket 移動後的路徑穩定性。
4. 實證通過後，才裁決是否新增 `Library/規範/15-內容篩選.md`、穩定 ID `content-selection` 與 `manage-content-selection` Skill。
5. 待裁決項目：`defer` 是否需要期限、素材級排除採 hook ID 或 `char_span`、大量裁決是否需要事件格式。

## 可重跑命令

```bash
node --test tools/registry/test/*.test.mjs
node --check tools/viz/generate.mjs
node --check tools/viz/parse-doc-artifacts.mjs
node --check tools/viz/render-inline-views.mjs
git diff --check
rg --hidden -n 'docs/superpowers|superpowers:|REQUIRED SUB-SKILL' . \
  --glob '!.git/**' \
  --glob '!Library/archive/**' \
  --glob '!.agents/handover/[0-9a-f]*.md'
```
