# 交接：AI 交易 Article 收錄與 registry Windows 同步修正

- 內容 commit：`49c78b5`
- 分支：`cursor/inline-diagram-viz-plan-2c21`
- 日期：2026-07-14

## 完成內容

- 收錄 X 原貼 `2072258423685599594` 指向的 Article「Wall Street Solved AI Trading in 1988. You're 37 Years Late.」；原文逐字保存為 10,741 個 Unicode 字元。
- 建立 `PENDING-wall-street-solved-ai-trading-in-1988` 的 `DOC/Review` 完整包：8 個模塊（5 個技能訊號、3 個背景）與 7 條有文本證據的結構邊。
- 因 Renaissance／Medallion 的績效與模型、Baum–Welch／HMM 的實際使用、統計 NLP 到現代 LLM 的沿革，以及「solved」／「99%」量化說法均未附可稽核來源，維持 `needs_review`；沒有把這些主張當作已驗證事實。
- 5 個技能訊號模塊已經 shadow `align`，均因空登記簿產生 `reject` 建議，寫入 `REVIEW-000010` 至 `REVIEW-000014`；沒有建立正式 SkillNode、待建槽或熟練度變更。
- 修正 `tools/registry/core/store.mjs`：Windows 對唯讀控制代碼執行 `fsync` 會回 `EPERM`，將同步時的 `open` 模式改為 `r+`。正式交易與完整測試已通過。

## 關鍵檔案

- 來源摘要與覆核原因：`DOC/Review/wall-street-solved-ai-trading-in-1988/README.md`
- 逐字原文：`DOC/Review/wall-street-solved-ai-trading-in-1988/source.md`
- 模塊與字元區間：`DOC/Review/wall-street-solved-ai-trading-in-1988/modules.md`
- 結構邊與驗收註記：`DOC/Review/wall-street-solved-ai-trading-in-1988/edges.md`
- 人工查找索引：`DOC/INDEX.md`
- shadow 對齊與稽核事件：`registry/reviews.jsonl`、`registry/events.jsonl`
- Windows 交易相容性：`tools/registry/core/store.mjs`

## 驗證

- 以公開 Article 回應逐字比對 `source.md`：相同，長度 10,741。
- 原文、8 個模塊、7 條邊與 5 筆 `REVIEW-000010`～`REVIEW-000014` 的關聯檢查：通過。
- `node --test tools/registry/test/*.test.mjs`：48/48 通過。
- `node tools/registry/internal/validate-store.mjs registry`：通過；計數為 nodes 0、edges 0、alignments 0、pending 0、reviews 14、events 14。
- 內容提交前 `git diff --cached --check`：通過。

## 接手與未決事項

1. 本文仍在 `DOC/Review`；人工需裁決正式文本 ID、主要類型，以及應轉 `DOC/Stocks` 或 `DOC/Archive`。
2. 若使用文中 HMM、Renaissance 或歷史沿革作學習證據，先補一手或權威來源；不可把「AI trading 已被解決」當作事實命題。
3. `REVIEW-000010`～`REVIEW-000014` 僅是 shadow 建議。依 `tools/registry/internal/decide-review.mjs` 具名人工覆核後，才能寫正式 alignment 或 pending。
4. 工作樹刻意保留且未納入 `49c78b5` 的既有工作：`DOC/Review/how-to-make-company-ai-native/` 的 README／selection、`DOC/Review/making-fable-cheaper-than-opus/`、`docs/plans/2026-07-14-module-selection-implementation.md`，以及其索引列。接手時不得覆寫或混入本來源提交。

## 可重跑命令

```bash
node --test tools/registry/test/*.test.mjs
node tools/registry/internal/validate-store.mjs registry
git show --check 49c78b5
git show --stat 49c78b5
```
