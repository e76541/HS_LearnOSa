# 交接：技能登記簿半自動化實作路線

- 內容 commit：`7830dba`
- 分支：`cursor/inline-diagram-viz-plan-2c21`
- 日期：2026-07-14

## 完成內容

- 新增技能登記簿半自動化實作路線，未實作登記簿程式或資料。
- 將自動化拆成兩道門：Gate A 先裁決凍結衝突；Gate B 待三十篇基準與 AOI 裁決後才考慮自動寫入。
- 規劃 `registry/` 真值、待建、覆核、對齊與稽核資料責任，以及 `tools/registry/` 的內部模組邊界。
- 固定對外只暴露 `align`、`pending`、`node_id`、`proficiency` 四操作；驗證器、索引器及覆核流程不得變成跨層新介面。
- 規劃 Phase 0–6：裁決與 fixture、schema／invariant、穩定 ID／人工熟練度、shadow-mode 對齊、覆核與 pending、管線接線、三十篇評估。
- 明列不自動升格 pending、不自動通過 `broader_than`、不自動寫熟練度、不修改 ModuleCore。

## 關鍵檔案

- `docs/superpowers/plans/2026-07-14-skill-registry-automation.md`
- 對照：`Library/規範/40-技能登記層.md`
- 對照：`Library/規範/50-驗收與度量.md`
- 對照：`Library/規範/70-版本與裁決.md`
- 對照：`.agents/skills/manage-skill-registry/SKILL.md`

## 驗證

- `git diff --check` 通過。
- 路線未修改 canonical、ModuleCore、模塊資料、邊資料或 `DOC/INDEX.md`。
- 對齊輸入限定 ModuleCore；背景與非技能訊號明確排除。
- `related`、降級命中、pending 升格及熟練度寫入均保留人工閘門。
- `broader_than` 與模塊圖隔離；alignment 明定為裁決記錄，不作圖邊。

## 接手入口

1. 先讀 `Library/CURRENT.md`，再依穩定 ID 讀 `module-layer`、`skill-registry`、`identity-terminology`、`validation`、`version-decisions`。
2. 讀 `.agents/skills/manage-skill-registry/SKILL.md`。
3. 讀本次實作路線，從 Gate A 開始，不得直接建立自動化程式。

## 後續順序

1. 由 AOI 裁決 `Library/規範/70-版本與裁決.md` 所記自動化實作前的上位凍結衝突。
2. Gate A 通過後，只先做 Phase 0–1：資料字典、fixture、schema、儲存 invariant 與測試。
3. 第一個實作 PR 不接模型、不接下游；第二個 PR 才做 shadow-mode `align`。
4. 三十篇基準完成前，不設定自動接受閾值，也不裁決 R10。

## 分支注意事項

- 本分支在本次內容提交前已有兩個尚未推送至遠端的既有提交：`ec11dee`、`753138b`；本次交接推送會一併推送。
- 本次未改寫或拆分上述既有提交。
