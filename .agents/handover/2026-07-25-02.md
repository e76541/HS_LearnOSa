# 交接快照 2026-07-25-02

承接 [2026-07-25-01](2026-07-25-01.md)；區間＝看板 **#70～#71**。

## 完成範圍

- **F3a 缺口驅動導航紙上試跑（試作，F／F3a）**：以使用者本次貼入的 **Graph Engineering 5-step 版**為材料（辨識出**與已收錄的 14-step 版不是同一篇**，同作者另一篇），跑完整一趟 `emergent` Journey。
  - 證據層（試作級，非正式收錄）：[source.txt](../../docs/experiments/assets/2026-07-25-ge-5step/source.txt) 10,742 字元／[14 模塊 N00–N13](../../docs/experiments/assets/2026-07-25-ge-5step/modules.md)／[15 條邊 E01–E15](../../docs/experiments/assets/2026-07-25-ge-5step/edges.md)。
  - 補丁 C 要求的**主軸識別 N=2 複驗**：匯聚最大點 N02（3 條）**成立**，但領先幅度僅 3:2、且 E03 改判即翻轉 → 維持啟發式定位，建議第三份再驗。
  - 走跑結果：4 站全 `emergent`、12 STEP（`decide`×5／`execute_next`×2／`q2_fill`×2／`resume_card`×1／`reroute`×1）、`steps_to_arrive` 2／5／1／2、尾段 2 步保持 `gate_id: null`；**封站四步的失敗分支真的跑到**（GATE-E2 首驗 2/3 未過 → 缺口留著不封 → 補知重驗才封）；中途新增 `no_evidence` 缺口 GAP-7（淨增 +1）。
  - 跑通程度：**✅18／🟡5／⛔2／⬜4**；報告 [2026-07-25-f3a-navigation-dry-run-spike.md](../../docs/experiments/2026-07-25-f3a-navigation-dry-run-spike.md) §7 逐條核對、§8 列 **9 項空洞（H1–H9）**。
- **呈現形式的兩次修正**：先以 archify 出靜態軌跡圖 [dry-run-trace.html](../../docs/experiments/assets/2026-07-25-ge-5step/dry-run-trace.html)（check 4/4 過）；經判定**該圖與既有流程圖重複、且軌跡本身是一條鏈**（只有一個分叉），改做**會動的回放頁**呈現狀態隨步驟改變。靜態圖保留供對照，未刪。
- **`journey-playback` 技能（實作，T／T3）**：把回放頁做成**殼／資料分離**的可複用零件，裝於使用者層 `~/.claude/skills/journey-playback/`——`SKILL.md`（何時用／何時改用 archify、資料格式、硬規則、檢查清單）＋`assets/template.html`（版面／播放器／原文對位邏輯）＋`bin/build.py`（組裝與擋錯）＋`examples/demo.playback.json`（8 拍最小範例）。
  - F3a 回放頁改為**由資料檔重建**：內容全在 [dry-run-playback.json](../../docs/experiments/assets/2026-07-25-ge-5step/dry-run-playback.json)（13.7 KB），HTML 降為產出物。
  - 原文對位：27 拍中 22 拍依 `char_span` 高亮（青＝模塊、綠＝驗收邊證據、紅＝原文沒有這段，即 GAP-7）。
  - `AGENTS.md` 領域 Skill 對照加一列（掛 T／T3，註明使用者層）。
- **修掉一個真 bug**：建置時 `__SOURCE_TEXT__` 佔位符把樣板 JS 內同名字串一併替換，整篇原文被塞進正則式導致頁面不動（即使用者回報的「卡住」版本）。`build.py` 現在檢查佔位符出現次數，同錯不會再過。

## 檢查結果

- **F 必做檢查 2/2**：①#70 摘要標明站位 F3a、主欄記作業模塊 `F` 未把站位當 ID；②未越界寫入 T／R／V 正式產物（試作只產出於 `docs/experiments/`）。
- **T 必做檢查**：`python tools/viz/html-hub.py build` 重建索引 → **7 筆 HTML 路徑全部存在**（含本次兩份新頁）；新增檔案**自動收錄已驗證**。搜尋／分類／預覽為 UI 行為、本次未改 `html-hub.py`，**未回歸測試**；C1 整批測試仍為暫取消狀態。
- **技能自檢**：樣板嵌入 JS 語法檢查 OK；F3a 頁重建後行為對帳與模板化前一致（27 拍／12 步／4 站／缺口 3 開 4 消／減少率 0.25／`steps_to_arrive` 2·5·1·2）；故意越界的 `span` 確實被 `build.py` 擋下並非零退出；demo 範例可建（8 拍／2 步／1 關卡）。
- **邊界**：未改 canonical、路線、作業模塊登記表；**未回寫任何模塊／邊真值**（14-step 版的 `DOC/Review/` 完全未動）；試作模塊 `N##` 與正式 `M##` 分屬不同 `source_id`，明文不互通。
- **flow-map 不波及**：站位、作業模塊邊界、代號範圍均未變。
- **交接本身未操作任何 Git 指令。**

## 未解事項

- **9 項空洞待裁**（報告 §8）。阻塞度最高兩項為 ⛔：**H3 驗收通過門檻無值**（幾題對算過）、**H6 缺口只引到 1～2 條邊時湊不滿「一趟 3～7 題」，且 `no_evidence` 缺口在文本型無素材可驗**。其餘 H1／H2／H4／H5／H7／H8／H9 為 🟡。
- **H6 有一半責任在切分不在規格**：本次只抽 15 條邊（平均一模塊 1.07 條）。建議把 5-step 文本**正式收錄（F1→F2）**、補到每個缺口至少引得到 3 條邊，再用**同一份抵達判準重跑同一趟**，用證據判斷 H6 是否消失。
- **未測**：`planned` 站、專案型終點（`evidence_mode: project`）。
- **關卡密度爭議（未裁）**：一次決定要過十四關，其中承載誠實的只有三關（先寫 `pass_rule`／獨立驗收／留痕）。已口頭提「砍成三關」方向——若裁可，H2／H4／H7／H8／H9 五項空洞會直接消失。**尚未寫成修訂提案。**
- **技能落點待裁**：`journey-playback` 只在使用者層，換機器需重裝；是否在 `.agents/skills/` 另放一份未定（我建議不放，兩份 SKILL.md 會漂）。
- **承接前次未結**：GE 14-step 文本 `needs_review` 三項（正式 `text_id` 未裁等）；看板 #22（T／C1 五份並行預覽）進行中、C1 測試暫取消；整合總綱六衝突、soft route 試作欄位、路線 ADJ-001～005／INI-004 Gate B／INI-005 Phase 1、導航 OSA §11 1–6；變更表 #20 編號重複（歷史瑕疵不回改）；`tools/viz/__pycache__/` 未追蹤（**勿提交**）。
- **本區間全部變更待「推送」**，含前次快照後仍未推的區間。

## 下一步

1. **裁 H3／H6**（或授權把「最小關卡集」寫成草案修訂提案，不動已裁的八項）。
2. **正式收錄 5-step 文本**（F1→F2），補足邊密度後**重跑同一趟**，驗證 H6 是否為切分造成。
3. 補跑 `planned` 站與專案型終點各一趟，把 ⬜ 四項降掉。
4. 執行「推送」。

## 完整變更時間表

| 序 | 事項 | 落點 |
|---|---|---|
| 1 | 讀規範／記憶／草案兩份（抽出稿＋缺口驅動），確認材料是**新文本**而非已收錄的 14-step 版 | — |
| 2 | 存原文 `source.txt`（10,742 字元），以 Python 算章節字元位移 | `docs/experiments/assets/2026-07-25-ge-5step/` |
| 3 | 切 14 個試作模塊 N00–N13（標 `spike_only`，與 `M##` 分命名空間） | `modules.md` |
| 4 | 抽 15 條邊 E01–E15，四型齊備；同檔做主軸識別 N=2 複驗（N02 勝出 3:2） | `edges.md` |
| 5 | 走完整一趟 emergent Journey：Journey 十欄／7 缺口／4 GATE／12 STEP／5 張 DecisionCard 正反面／每站逐題驗收與模擬作答 | 試跑報告 §1–§6 |
| 6 | 逐條核對規格（✅18／🟡5／⛔2／⬜4），列 9 項空洞 | 試跑報告 §7–§8 |
| 7 | 試作紀錄 INDEX 加一列；看板 #70 | `docs/experiments/INDEX.md`、`ops-board.md` |
| 8 | archify sequence 軌跡圖（render＋check 4/4 過） | `dry-run-trace.html`／`.sequence.json` |
| 9 | 判定該圖與既有流程圖重複 → 改做回放頁；手寫單頁 HTML（播放器＋缺口狀態機＋帳） | `dry-run-playback.html` |
| 10 | 接原文對位：`char_span` 高亮＋自動捲動，三色語義；以 Python 注入原文確保位移一致 | 同上 |
| 11 | 抽成 `journey-playback` 技能：樣板／`build.py`／demo；F3a 頁改由 JSON 重建 | `~/.claude/skills/journey-playback/`、`dry-run-playback.json` |
| 12 | 修佔位符污染樣板 JS 的 bug，`build.py` 加佔位符次數檢查；重建後對帳一致 | `build.py`、`template.html` |
| 13 | `AGENTS.md` Skill 對照加一列；看板 #71；試作報告資產清單補回放頁與資料檔 | `AGENTS.md`、`ops-board.md`、試跑報告 |
| 14 | 跑 F／T 必做檢查；`html-hub.py build` 重建索引（7 筆全存在） | `docs/html-hub.html` |
| 15 | 建本快照、更新交接紀錄 | `.agents/handover/`、`current-handover.md` |
