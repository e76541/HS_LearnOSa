# 交接：收錄後處置 Shadow 試行

- 內容 commit：`30c5f58`
- 分支：`codex/post-intake-disposition-trial`
- 日期：2026-07-14

## 完成內容

- 將外部《來源收錄後處置整合整理》轉為 **non-canonical shadow 試行**；沒有移植其 `Source/*`、`SourceRegistry`、只保存出口或八態狀態機。
- 在四個既有 `DOC/Review` 文檔包共置 `post-intake-disposition.md`：AI-Native 的身份裁決、Loop 與 `SRC-002` 的來源比較、Quant Trading 的平行文檔比較，以及 Fable 的可復現資料缺口。
- 新增只讀解析／驗證工具與 fixtures。它只鏡像既有 `Review`／`needs_review` 身份，檢查四欄關聯表格、候選上限、repo 內候選路徑與常見秘密值模式；不保存新處置 enum、沒有狀態轉移，也不回寫任何真值。
- 新增 shadow 設計與測試矩陣；敏感資訊防線明定為常見模式的 fail-closed 輔助，不能取代人工判讀。

## 關鍵檔案

- 設計：[docs/specs/2026-07-14-post-intake-disposition-trial.md](../../docs/specs/2026-07-14-post-intake-disposition-trial.md)
- 測試矩陣：[docs/plans/2026-07-14-post-intake-disposition-trial-test.md](../../docs/plans/2026-07-14-post-intake-disposition-trial-test.md)
- 工具：[tools/post-intake-disposition-trial/README.md](../../tools/post-intake-disposition-trial/README.md)
- 四個試點：`DOC/Review/*/post-intake-disposition.md`

## 驗證

- `node --test tools/post-intake-disposition-trial/test/*.test.mjs`：6/6 通過。
- `node tools/post-intake-disposition-trial/validate-disposition.mjs DOC/Review`：4 個試點通過。
- `node --test tools/registry/test/*.test.mjs`：48/48 通過。
- `node --check`（兩個新 `.mjs`）、`git show --check 30c5f58`：通過。
- 雙軸審查後已移除多餘 `evidence_state` enum，並讓關聯候選區的非表格文字 fail closed。

## 不變邊界

- 未改 `Library/規範/`、`.agents/skills/ingest-text/`、`DOC/INDEX.md`、`source.md`、`modules.md`、`edges.md` 或技能登記真值。
- 關聯候選不是模塊層邊；候選比較不授權合併、移動、刪除或新增下游資產。

## 後續

1. 以更多實際新來源累積 shadow 註記與人工裁決資料；敏感內容仍先停在人工保護流程，不把秘密值帶入紀錄。
2. 由人工裁決 Loop 是否為 `SRC-002` 的版本、Quant 與 Wall Street 是否有閱讀關聯，以及各 Review 文檔的正式 ID／bucket。
3. 僅在有基準結果與裁決後，才評估是否修訂 canonical 規範或 `ingest-text` Skill。
