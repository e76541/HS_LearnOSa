# 交接：Shadow 技能登記簿與兩篇文本收錄

- 內容 commit：`6d3a35e`
- 分支：`cursor/inline-diagram-viz-plan-2c21`
- 日期：2026-07-14

## 完成內容

- 實作技能登記簿 Phase 0–5 與 Phase 6 評估基建：六種 JSON schema、交易式儲存、穩定 ID、人工熟練度管理、shadow 對齊、人工覆核、pending、只讀管線適配器與 Gate B 評估器。
- 公開介面維持凍結的 `align`、`pending`、`node_id`、`proficiency` 四操作；正式節點、別名、`broader_than`、pending 升格與熟練度仍不能由系統自動寫入。
- canonical 規範更新至 v0.3 r5：r3 裁決 shadow 自動化範圍；r4 要求命中與未命中逐項列表；r5 修正閱讀層以中文技能詞與英文正名為主，模塊 ID 只作證據溯源。
- 完成 `Loop and Harness Engineering` 文檔包：16 模塊、14 條邊，因與 canonical SRC-002 描述不一致而留在 Review。
- 完成 `Quant Trading Is Not Prediction` 文檔包：9 模塊、10 條邊；9 個技能訊號已做 shadow 對齊，產生 9 筆待人工覆核案及人類可讀的技能詞語草案列表。
- `DOC/INDEX.md` 已登記兩篇 Review 文檔；可重建 embedding JSON 已排除版控，只保留 `.gitkeep`。

## 登記簿目前狀態

| 類別 | 數量 | 說明 |
|---|---:|---|
| 正式 SkillNode | 0 | 系統未越權建立節點 |
| 正式 `broader_than` | 0 | 無人工核准關係 |
| 正式 alignment | 0 | 9 筆均仍待人工覆核 |
| pending | 0 | reject 尚未經具名人工確認 |
| review | 9 | `REVIEW-000001` 至 `REVIEW-000009` |
| audit event | 9 | shadow 建議寫入事件 |

本篇量化交易的待建詞語草案見 `DOC/Review/quant-trading-is-not-prediction/README.md`，包含「資產特定交易優勢隔離」、「殘差均值回歸套利」、「量化策略過度擬合診斷」等九項。草案不是正式節點。

## 關鍵檔案

- 規範入口：`Library/CURRENT.md`
- 登記 canonical：`Library/規範/40-技能登記層.md`
- 登記 Skill：`.agents/skills/manage-skill-registry/SKILL.md`
- 資料字典：`tools/registry/DATA_DICTIONARY.md`
- 操作說明：`tools/registry/README.md`
- 正式／流轉資料：`registry/*.jsonl`
- 自動化計畫：`docs/superpowers/plans/2026-07-14-skill-registry-automation.md`
- 文檔包：`DOC/Review/loop-and-harness-engineering/`、`DOC/Review/quant-trading-is-not-prediction/`

## 驗證

- `node --test tools/registry/test/*.test.mjs`：48/48 通過。
- `node tools/registry/internal/validate-store.mjs registry`：通過；0 nodes、0 edges、0 alignments、0 pending、9 reviews、9 events。
- `node tools/registry/internal/evaluate-alignments.mjs tools/registry/test/benchmark`：評估器可重跑；Gate B 正確為 false，資料為 1/30 篇、0/3 observed cases、R10 未裁決、自動接受閾值未設定。
- 兩個文檔包合計 25 模塊、24 條邊；模塊區間連續、邊端點存在、三個 DOC 索引連結有效。
- 量化交易列表含 9 個唯一且格式合法的英文正名草案；人類閱讀表以技能詞語為主。
- `git diff --check` 對非原文證據檔通過。兩個 `source.md` 保留附件原始空白以維持字元區間，不作格式清理。

## 接手與未決事項

1. 讀 `Library/CURRENT.md`，再讀 `skill-registry`、`module-layer`、`identity-terminology` 與 `validation` canonical 規範。
2. 讀 `.agents/skills/manage-skill-registry/SKILL.md`；正式資料修改必須經 `tools/registry/core/store.mjs`，不得直接手改 JSONL。
3. 由具名人工逐筆裁決 `REVIEW-000001` 至 `REVIEW-000009`。若確認均非既有詞語，才執行 reject 進 pending；不可因登記簿為空便自動建節點。
4. 人工裁決兩篇 Review 文檔的正式文本 ID、主要類型與 Stocks／Archive 歸屬；量化交易文中的 Renaissance／Medallion 歷史與績效說法仍需補來源或降權。
5. Gate B 尚未通過：累積三十篇人工基準與 shadow 觀測、完成 R10 裁決前，不得設定自動接受閾值或開放正式自動寫入。

## 可重跑命令

```bash
node --test tools/registry/test/*.test.mjs
node tools/registry/internal/validate-store.mjs registry
node tools/registry/internal/review-queue.mjs registry
node tools/registry/internal/evaluate-alignments.mjs tools/registry/test/benchmark
```
